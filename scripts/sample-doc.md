# 冒烟脚本演示样本：VPN 故障处理手册（V1）

## 认证超时

出差员工反映认证超时，通常与本地缓存过期或服务端会话超时有关。

处理步骤：

1. 让员工尝试重新认证，观察报错码。
2. 清理本机 VPN 客户端缓存。
3. 若仍失败，检查服务端会话超时配置，默认 15 分钟。

## 日志排查

在客户端日志目录查找 `auth-timeout` 关键字；日志级别建议调整为 DEBUG 以便定位。

## 引用说明

本文按标题分块后，检索命中会返回 `article:{id}/version:{v}#c{n}` 格式的锚点引用。
